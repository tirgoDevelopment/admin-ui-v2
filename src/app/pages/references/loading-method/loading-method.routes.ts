import { Route } from '@angular/router';
import { LoadingMethodComponent } from './loading-method.component';

const routes: Route[] = [
  {
    path: '',
    component: LoadingMethodComponent,
  },
];

export default routes;
