import { Route } from '@angular/router';
import { CargoTypeGroupsComponent } from './cargo-type-groups.component';

const routes: Route[] = [
  {
    path: '',
    component: CargoTypeGroupsComponent,
  },
];

export default routes;
