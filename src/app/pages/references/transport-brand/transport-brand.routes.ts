import { Route } from '@angular/router';
import { TransportBrandComponent } from './transport-brand.component';

const routes: Route[] = [
  {
    path: '',
    component: TransportBrandComponent,
  },
];

export default routes;
