import { Route } from '@angular/router';
import { TransportKindsComponent } from './transport-kinds.component';

const routes: Route[] = [
  {
    path: '',
    component: TransportKindsComponent,
  },
];

export default routes;
